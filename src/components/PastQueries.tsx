"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const PastQueries = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>📜 Past Queries</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is where your past queries and their answers will be displayed.</p>
        {/* TODO: Implement display of past queries */}
      </CardContent>
    </Card>
  );
};
